# Éditeur — 2026-08-15

**Édition non produite.** Arrêt avant toute dépense. Coût engagé : **0,00 $**.

## Ce qui a été fait

1. Lecture de `CLAUDE.md` et de `.claude/skills/barometre-editorial/SKILL.md`.
2. Vérification du coupe-circuit, bucket `index` (étape 1 du brief).
3. Inventaire des identifiants disponibles dans l'environnement d'exécution.
4. Constat de blocage, arrêt, rédaction de ce compte-rendu.

Aucun appel LLM n'a été émis. Aucun événement `mentio/index.refresh` n'a été envoyé.

## Ce qui a échoué

### 1. Les deux modèles mesurés de `weekly-index` sont inactifs

`src/inngest/functions/weekly-index.ts:77` restreint la mesure à `chatgpt` et
`gemini`. Les deux clés attendues par `src/lib/models.ts:25-26` sont absentes :

| Variable | État |
|---|---|
| `OPENAI_API_KEY` | absente |
| `GOOGLE_GENERATIVE_AI_API_KEY` | absente |
| `PERPLEXITY_API_KEY` | absente |
| `ANTHROPIC_API_KEY` | présente |
| `OPENROUTER_API_KEY` | présente |

`activeProviders()` ne renvoie donc aucun des deux modèles mesurés : la phase 1
collecterait zéro réponse et `save-edition` lèverait « Édition abandonnée : aucun
run exploitable » (`weekly-index.ts:231`). Le garde-fou ferait son travail.

Les deux clés présentes — Anthropic et OpenRouter — sont des **modèles de
traitement** (juge, rédaction). Les substituer aux modèles mesurés est interdit par
la constitution §7 : le produit vend « ce que ChatGPT répond à vos clients ».
Aucune substitution n'a été tentée.

### 2. Supabase injoignable — et le coupe-circuit donne un feu vert qu'il ne peut pas étayer

`NEXT_PUBLIC_SUPABASE_URL` est définie mais **vide** ; `SUPABASE_SERVICE_ROLE_KEY`
est présente. `supabaseAdmin()` lève donc `supabaseUrl is required` à chaque appel.

Conséquence à signaler, vérifiée en exécution :

```
lecture brute a levé : supabaseUrl is required.
mois  : 0 $
jour  : 0 $
guard : {"allowed":true,"bucket":"index","spentUsd":0,"capUsd":3}
```

`spentTodayUsd()` et `spentThisMonthUsd()` avalent l'erreur de lecture et renvoient
`0` — c'est délibéré (`spend-guard.ts:76-80` : « un faux positif coûterait un scan
raté à un prospect »). Mais lorsque la base est *totalement* injoignable, ce choix
transforme le coupe-circuit en feu vert permanent : `guard("index")` répond
`allowed: true` à 0 $ dépensé sans avoir rien pu lire.

Ce « ok » n'a donc **aucune valeur informative ici**. Le brief prévoit l'arrêt si le
coupe-circuit refuse ; un coupe-circuit inévaluable est un cas plus prudent encore,
pas moins. Déclencher la mesure sur la foi de ce feu vert reviendrait à engager
~2,50 $ à l'aveugle, sans pouvoir vérifier ensuite l'édition en base (étape 3 du
brief : édition non vide, mouvements > 10 places, intervalles de confiance).

Sans base, par ailleurs : pas de chargement des 50 questions (`load-prompts`), pas
d'écriture de l'édition, pas de relecture.

### 3. Pas de chemin de déclenchement vérifiable

`INNGEST_EVENT_KEY` est présente, `INNGEST_SIGNING_KEY` absente. Aucun `.env.local`,
aucun serveur de dev Next, aucun serveur Inngest local. Le seul déclenchement
techniquement possible serait un POST vers l'Inngest de production, qui ferait
tourner la mesure sur l'application déployée — donc une dépense réelle, engagée
sans validation préalable (constitution §7 et §8.3) et invérifiable depuis ici.
Non fait.

## Ce qui n'avait pas lieu d'être fait

`llms.txt`, `llms-full.txt` et `/barometre.md` ne sont pas des fichiers statiques :
ils sont générés à la demande depuis la base par `src/lib/markdown-twin.ts`, via
`getEditions()`. Ils se mettent donc à jour d'eux-mêmes dès qu'une édition existe.
L'étape 5 du brief ne demande aucun commit tant qu'aucune édition n'est produite.

## Conséquence

Constitution §4 : **ne jamais publier une édition vide, mieux vaut garder la
précédente.** L'édition précédente reste en ligne, inchangée. Aucun chiffre publié
n'a été touché.

## Ce qu'il faut pour débloquer

1. `NEXT_PUBLIC_SUPABASE_URL` renseignée (elle est vide, pas oubliée) ;
2. `OPENAI_API_KEY` et `GOOGLE_GENERATIVE_AI_API_KEY` ;
3. le feu vert du fondateur sur la dépense — estimation : **~2,50 $**
   (1 passage × 50 questions × 2 modèles, puis 4 passages sur ≤ 12 questions
   disputées), sous plafond quotidien `index` de 3,00 $ et plafond mensuel de 15 $.

## Point à arbitrer, hors périmètre de cet agent

Le coupe-circuit ne distingue pas « rien dépensé » de « rien lu ». Un `guard()` qui
ne peut joindre la base répond `allowed: true`. Sur le bucket `index`, qui tourne
sans surveillance humaine par cron le dimanche à 8 h, l'arbitrage mériterait d'être
revu : échouer ouvert protège un prospect d'un scan raté, mais laisse une édition
hebdomadaire dépenser sans compteur. Aucun changement n'a été fait — ce n'est pas le
rôle de l'Éditeur de toucher au coupe-circuit.
